package org.javacord.api.listener.webhook;

import org.javacord.api.entity.webhook.Webhook;

/**
 * This is a marker interface for listeners that can be attached to a {@link Webhook}.
 */
public interface WebhookAttachableListener {
}