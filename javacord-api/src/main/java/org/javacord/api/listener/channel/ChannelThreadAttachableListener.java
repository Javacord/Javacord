package org.javacord.api.listener.channel;

import org.javacord.api.entity.channel.ChannelThread;

/**
 * This is a marker interface for listeners that can be attached to a {@link ChannelThread}.
 */
public interface ChannelThreadAttachableListener
        extends TextChannelAttachableListener {
}
